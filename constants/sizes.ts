import { windowWidth } from "./Layout";

export default {
  radius: {
    sm: windowWidth / 40,
    md: windowWidth / 20,
    lg: windowWidth / 10,
  },
  padding: {
    sm: windowWidth / 100,
  },

  width: {
    card: {
      full: windowWidth / 1.1,
    },
  },
};
